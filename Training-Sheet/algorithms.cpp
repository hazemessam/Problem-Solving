// sorting
for(int i=0 ; i<size-1 ; i++)
        for(int j=i+1 ; j<size ; j++)
            if(cards[i] < cards[j])
                swap(cards[i], cards[j]);
//////////////////////////////////
// str to int or float
#include<string>
using namespace std;
int main()
{
    string str = "123";
    int i = stoi(str);
    int f = stof(str)
}
//////////////////////////////////
